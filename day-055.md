# Day 55 – 24/02/2026

- Instead of using the zustand persist middleware, opted to manually do it instead since the middleware has a bunch of limitations and does not act as I wish.
- Using idb-keyval library to work with indexedDB as it is quite simple to use